{
  "name": "diffy",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "rollup -c config/rollup.js --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "devDependencies": {
    "rollup": "^0.33.0",
    "rollup-watch": "^2.4.0"
  }
}
